#include "holberton.h"
#include <stdio.h>

/**
 * main - write a program that prints name of the file,
 * it was compiled from then new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
